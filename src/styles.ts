import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        borderRadius: 50,
        elevation: 4,
        justifyContent: 'center',
        alignItems: 'center',
        alignSelf: 'center',
        zIndex: 10001
    },
    label: {
        fontSize: 14,
        color: '#111'
    }
});
