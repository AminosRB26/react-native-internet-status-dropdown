import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        borderRadius: 50,
        elevation: 4,
        justifyContent: 'center',
        alignItems: 'center',
        alignSelf: 'center',
    },
    label: {
        fontSize: 14,
        color: '#111'
    }
})